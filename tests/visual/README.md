# Visual regression

Baselines: `tests/visual/mockups.spec.ts-snapshots/` (commit these).

Run: `bun run test:visual`
Update baselines: `bun run test:visual:update`

Failures write diff PNGs to `test-results/`.
